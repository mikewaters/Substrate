# Typescript/javascript development

---

# Release
You can only invoke these commands if specifically instructed to prepare a release.

- Run all checks: `just check` (runs lint, format, type check)
- Run all tests: `just test`

---
